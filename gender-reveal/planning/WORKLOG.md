# 작업 이력 (Work Log)

`gender-reveal` 프로젝트에서 세션별로 진행된 요청사항과 결정사항을 시간순으로 기록합니다.
새 세션에서 작업할 때는 이 파일 맨 아래에 새 항목을 이어서 추가합니다 (기존 항목은 수정하지 않음).

## 2026-09-17

### 1. 프로젝트 초기화 (`/init`)
- 저장소에 요구사항 문서([Requirements.md](Requirements.md))만 있는 초기 상태를 확인
- [CLAUDE.md](../CLAUDE.md) 최초 생성 — 프로젝트 상태, 기능 아이템, 화면 흐름 요약

### 2. Requirements.md 구체화 (참고사이트 분석)
- 참고사이트(https://hyeminpark9105.github.io/baby-gender-reveal/) 소스 코드 직접 분석
  (`config.js`의 `boy`/`girl`/`secret` 상태, 인트로 타이핑 연출, 선물상자 리빌 연출 구조 파악)
- 이를 기반으로 기능 아이템 5종(페이지 작성/웹주소 생성/공유/결과 대시보드/보관주기)과
  화면 프로세스 4단계(인트로/선택/결과 확인/축하글)를 세부 항목까지 구체화
- 게스트 참여형 맞추기, 방명록, 관리자 대시보드, 보관주기 등 참고사이트에 없는 차별점을 명시

### 3. 미정 사항(TBD) 1차 확정 (재질문 후 반영)
- 소유자 인증 방식 → **매직링크** (이메일 링크 클릭, 비밀번호 없음)
- "이메일 전달" 기능 → **소유자 본인**에게 발행 링크 자동 발송 (게스트 초대 기능 아님)
- 보관주기 만료(30일) 후 → **열람만 제한**, 데이터는 보관, 로그인 후 연장 가능
- 리빌 테마 → 기존 선물상자 포함 **총 4개**(선물상자/케이크/풍선/토퍼) 중 선택
- 띠 반영 → **출산예정일 기준 자동 계산**, 미입력 시 문구 생략

### 4. 미정 사항(TBD) 2차 확정 + 테마 통합 결정
- 3종 신규 테마(케이크/풍선/토퍼) → 1차 출시 범위 포함, 에셋 제작 필요로 확정
- 만료 페이지 연장 정책 → **1회 한정 30일** (최대 총 60일, 재연장 불가)
- 커스텀 슬러그(웹주소) → **지원**
- "선물상자 + 토퍼/서프라이즈 박스가 유사하니 합칠 것" 요청 → 재질문 후
  **"박스 오픈 + 풍선/토퍼 결합"** 방식으로 통합, 명칭은 **"서프라이즈 박스"**로 확정 (테마 4종 → 3종)

### 5. BGM 자동재생 UX 확정
- 무음향 자동재생 + 음소거 해제 버튼 기본안 채택
- 재생 시점 **결과 확인 화면 진입 시부터**, 해제 상태는 브라우저에 저장되어 재방문 시에도 유지
- 이로써 Requirements.md의 모든 TBD 항목 해소

### 6. 추가 요청사항 반영 — 12지신 캐릭터 이미지
- "인트로 타이핑 문구에 출산예정일 기준 띠(12지신) 캐릭터 이미지도 조합" 요청
- 재질문 후: 타이핑 문구 옆/위에 **정지 이미지**로 노출, **인트로 + 결과 확인 화면 모두**에 동일 이미지 사용으로 확정
- 12지신 캐릭터 일러스트 12종 세트 제작 필요 사항을 Requirements.md에 명시

### 7. 와이어프레임 제작
- Requirements.md 기준 저충실도 와이어프레임을 Claude Artifact(Design Canvas)로 제작
- 화면 9개: 만료 안내 / 비밀 상태 / ①인트로 / ②선택 / ③결과 확인(서프라이즈 박스) / ④축하글 /
  결과 확인 테마 변형(케이크·풍선) / 관리자 대시보드
- 인트로→선택→결과 확인(서프라이즈 박스) 3개 화면은 실제 클릭 이동되는 프로토타입 링크로 연결
- 라이브 링크: https://claude.ai/artifact/FnyMrMcbQ8mYLnyHVRfoRV

### 8. 카운트다운 타이머 추가 요청 → 취소
- 비밀 상태 화면에 공개 예정 일시까지 카운트다운 타이머(D-day + 시:분:초) 추가 작업 진행 중
- 사용자가 작업 중단 후 **요청 취소** — 원래 상태("카운트다운 검토 중" placeholder)로 되돌림, 미반영

### 9. 산출물 프로젝트 경로 정리
- 와이어프레임 소스 파일을 스크래치 디렉터리에서 프로젝트 메인 경로로 이동
- `wireframe/README.md`(라이브 링크 안내) + `wireframe/project/*.dc.html`, `canvas.json` 저장
- [CLAUDE.md](../CLAUDE.md)에 wireframe 폴더 안내 추가

### 10. 작업 이력 파일 생성
- 지금까지의 진행 과정과 요청사항을 정리한 본 파일(`WORKLOG.md`) 생성
- 이후 세션에서도 이 파일에 작업 이력을 이어서 기록하기로 함

### 11. Claude 관련 디렉토리를 ai-work/ 아래로 통합
- "AI(Claude)로 작업/결과물을 만드는 상황이니 별도 디렉토리로 관리해달라"는 요청에 따라
  `Requirements.md`, `WORKLOG.md`, `wireframe/`를 프로젝트 루트에서 **`ai-work/`**로 이동
  (`CLAUDE.md`는 Claude Code가 프로젝트 루트에서 자동으로 읽는 파일이라 루트에 유지)
- 이동 후 루트에 원래 있던 빈 디렉토리 `context/`, `memory/` 발견 — git 미추적, 세션 중간에 생성된
  것으로 확인되어 용도가 불확실함을 사용자에게 알리고 확인
- 사용자가 이 두 디렉토리도 `ai-work/`로 함께 이동하기로 결정 → `ai-work/context/`, `ai-work/memory/`로 이동
- `WORKLOG.md`, `CLAUDE.md` 내 상대 경로 링크를 새 위치 기준으로 수정

### 12. `~/.claude/projects/...` 경로는 통합하지 않기로 결정
- "작업 영역은 한 곳이어야 한다"는 요청으로 Claude Code 앱의 세션 메모리 경로
  (`~/.claude/projects/-Users-hanwha-Workspace-event-echo-gender-reveal/`)도 `ai-work/`로 합쳐야 하는지 확인
- 해당 경로를 조사한 결과, 이 세션의 대화 로그(`.jsonl`)만 존재하고 `memory/` 폴더는 아직 생성되지 않은 상태를 확인
- 이 경로는 저장소 밖(홈 디렉토리)에 있는 **Claude Code 앱 자체의 로컬 상태**(세션 로그, 크로스세션 메모리)이며,
  경로도 앱이 프로젝트 디렉토리 기준으로 자동 관리하므로 옮겨도 실제로는 재사용되지 않고,
  대화 전문/개인 협업 메모가 git 저장소에 커밋될 위험만 있음을 설명
- 사용자가 이해하고 통합하지 않기로 결정 → 대신 [CLAUDE.md](../CLAUDE.md)에 "이 경로는 프로젝트 산출물이 아니며
  저장소에 포함되지 않는다"는 안내 한 줄 추가

### 13. 의미 없는 빈 디렉토리 `context/`, `memory/` 삭제
- 실제 Claude Code 메모리/세션 저장소는 `~/.claude/projects/...`(저장소 밖)에 있다는 게 확인되자,
  `ai-work/context/`, `ai-work/memory/`(11번 항목에서 이동해온 빈 디렉토리)는 쓰임이 없다는 점을 사용자가 지적
- 삭제 직전 재확인해도 여전히 완전히 빈 디렉토리(파일 없음, git 미추적)임을 확인 후 삭제
- 현재 `ai-work/`에는 `Requirements.md`, `WORKLOG.md`, `wireframe/`만 남음

### 14. `ai-work/` → `planning/` 이름 변경
- "앞으로 이 프로젝트에서 AI로 소스코드까지 개발할 건데, 'ai-work'라는 이름은 (누가 만들었는지를
  가리키는) 이름이라 어울리지 않는다"는 지적
- 이 폴더는 계속 기획/문서 전용으로 유지하고, 소스코드는 프로젝트 루트(또는 별도 `src/`)에 들어가는
  것으로 범위를 재확인
- "누가 만들었는가(ai-work)"가 아니라 "무엇이 들어있는가"를 드러내는 이름으로 바꾸기로 하고
  `planning`으로 확정 → `git mv ai-work planning`으로 이력 보존하며 이름 변경
- `CLAUDE.md`, `WORKLOG.md` 내 `ai-work/` 경로 참조를 전부 `planning/`으로 갱신
  (단, 위 9~13번 항목처럼 이름 변경 **이전** 시점을 서술하는 과거 기록은 당시 실제 이름인
  `ai-work/` 그대로 유지)
